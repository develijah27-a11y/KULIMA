'use client';

// Prompts for manual installation are intentionally disabled so the app
// behaves seamlessly like an authentic mobile application without nagging the user
// with unsolicited "Tap Share and Add to Home Screen" or link-sharing banners.
export function InstallPrompt() {
  return null;
}
