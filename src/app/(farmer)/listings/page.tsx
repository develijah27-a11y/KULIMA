'use client';

import { useToast } from '@/components/ui/Toast';

export function ListingsClient(_props: any) {
  const { showToast } = useToast();
  // Client wrapper for listings — actual logic lives in server component page
  return null;
}
